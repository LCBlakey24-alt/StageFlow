# Environment setup

When Supabase is added, Vercel will need these environment variables:

VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY

Keep service-role keys private. Do not put secret keys into frontend code.
